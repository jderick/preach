# YOU MUST CONFIGURE THESE VARS PRIOR TO RUNNING

setenv PREACH_ROOT `pwd`
setenv PREACH_SSH "ssh"
setenv PREACH_TEMP /tmp
setenv ERLANG_ROOT /some/path/to/otp_src_R13B03
setenv BINDIR $ERLANG_ROOT/bin
setenv PATH ${PREACH_ROOT}:${PREACH_ROOT}/Intel:${BINDIR}:${ERLANG_ROOT}/bin/x86_64-unknown-linux-gnu:${PATH}

# May be necessary if mutiple users are running jobs on same machines
#setenv ERL_EPMD_PORT 4370

rehash

